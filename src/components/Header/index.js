const React = require('react');
const Router = require('react-router');
const Classes = require('./styles.scss');

module.exports = () => (

    <div>
        <h1>Kegbot</h1>
    </div>

);
